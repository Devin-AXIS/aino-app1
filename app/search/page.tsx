import { SearchView } from "@/components/future-lens/views/search-view"

export const metadata = {
  title: "Search | Future Lens",
  description: "Search for signals, trends, and insights.",
}

export default function SearchPage() {
  return <SearchView />
}
