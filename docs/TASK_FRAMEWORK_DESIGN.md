# 任务框架系统设计

> 目标：任务有基本框架（约束），但卡片数量和种类完全由 AI 决定

---

## 🎯 核心设计理念

### 任务框架 = 约束 + 自由度

```
任务框架（用户定义）
  ├── 基本结构（必须）
  │   ├── 任务配置（监控范围、规则等）
  │   └── 任务总结卡片框架（可选，AI决定数量和种类）
  │
  └── 事件框架（必须）
      ├── 事件配置（基本信息）
      └── 事件卡片框架（可选，AI决定数量和种类）
```

### AI 自由度

- ✅ **卡片数量**：AI 决定（1-50张都可以）
- ✅ **卡片种类**：AI 决定（可以使用现有卡片，也可以创建新卡片）
- ✅ **卡片顺序**：AI 决定
- ✅ **卡片内容**：AI 生成

### 框架约束

- ✅ **任务必须有配置**：监控范围、规则等
- ✅ **事件必须有配置**：基本信息、时间等
- ✅ **卡片必须符合格式**：统一的 CardInstance 格式
- ✅ **卡片必须可渲染**：使用通用组件或预定义组件

---

## 📐 框架结构设计

### 1. 任务框架（Task Framework）

```typescript
interface TaskFramework {
  // 基本配置（必须，用户定义）
  id: string
  name: string
  category: string
  monitorScope: {
    targets: string[]
    keywords: string[]
    channels: string[]
  }
  rules: {
    alertLevels: Record<string, string[]>
    updateFrequency: string
  }
  
  // 任务总结卡片框架（可选，AI填充）
  summaryCardFramework?: {
    // AI 可以决定：
    // - 需要哪些卡片（cardIds）
    // - 卡片数量（1-5张）
    // - 卡片种类（可以使用现有，也可以创建新类型）
    cardIds?: string[]  // AI 生成，可以为空
  }
  
  // 事件框架（必须，用户定义）
  eventFramework: {
    // 基本结构（必须）
    requiredCards?: string[]  // 可选：必须包含的卡片（如 event-header）
    
    // AI 自由度（AI填充）
    // - 卡片数量：AI决定
    // - 卡片种类：AI决定
    // - 卡片顺序：AI决定
  }
}
```

### 2. 事件框架（Event Framework）

```typescript
interface EventFramework {
  // 基本配置（必须，用户定义或AI生成）
  id: string
  name: string
  category: string
  taskId: string
  urgency?: string
  impact?: string
  
  // 卡片框架（AI填充）
  cardFramework: {
    // AI 可以决定：
    // - 卡片数量（1-50张）
    // - 卡片种类（现有卡片或新卡片）
    // - 卡片顺序
    // - 卡片内容
    cardIds: string[]  // AI 生成
  }
}
```

---

## 🎨 卡片系统设计（完全动态）

### 1. 类型系统（完全动态）

```typescript
// ✅ 完全动态，AI 可以生成任意类型
export type CardTemplateId = string

// 示例：
// - "event-header"（预定义）
// - "event-competitor-price-comparison"（AI生成）
// - "event-market-sentiment-analysis"（AI生成）
// - "task-custom-insight-001"（AI生成）
```

### 2. 卡片实例（统一格式）

```typescript
interface CardInstance {
  id: string                    // 唯一ID
  templateId: string            // 任意字符串，AI生成
  componentName: string         // "GenericCard" 或预定义组件名
  data: Record<string, unknown> // 卡片数据（AI生成）
  config?: CardConfig          // 卡片配置（如果使用通用组件）
  metadata: {
    category: string
    tags: string[]
    createdAt: string
  }
}
```

### 3. 卡片配置（通用组件使用）

```typescript
interface CardConfig {
  layout: "comparison" | "list" | "grid" | "chart" | "text" | "custom"
  fields: {
    [key: string]: {
      type: "text" | "number" | "chart" | "list" | "custom"
      label: string
      position: string
      style?: any
    }
  }
  chart?: {
    type: "bar" | "line" | "pie" | "radar" | "custom"
    data: any
  }
  style?: {
    theme?: "default" | "highlight" | "warning" | "success"
    size?: "small" | "medium" | "large"
  }
}
```

---

## 🤖 AI 工作流程

### 场景1：用户创建新任务

```
1. 用户填写任务框架：
   - 任务名称：竞品监控
   - 监控目标：竞品A、竞品B
   - 监控关键词：价格、产品、融资
   - 监控渠道：官网、社交媒体

2. AI 分析任务需求：
   - 理解任务类型（竞品监控）
   - 理解监控范围
   - 理解业务目标

3. AI 生成任务总结卡片：
   - 决定需要3张卡片：
     * task-monitor-scope（监控范围）
     * task-statistics（统计）
     * task-trend（趋势）
   - 生成卡片数据

4. AI 定义事件框架：
   - 决定事件必须包含的卡片（如 event-header）
   - 决定事件可选卡片类型
```

### 场景2：任务生成新事件

```
1. AI 检测到新事件：
   - 竞品A发布低价套餐

2. AI 分析事件内容：
   - 事件类型：价格调整
   - 紧急度：高
   - 影响：高
   - 需要分析维度：价格对比、市场影响、客户流失

3. AI 决定卡片策略：
   - 需要13张卡片：
     * event-header（必须）
     * event-quick-read（必须）
     * event-core-insight（必须）
     * event-competitor-price-comparison（AI生成，新类型）
     * event-market-impact-analysis（AI生成，新类型）
     * event-customer-churn-prediction（AI生成，新类型）
     * ...（其他卡片）

4. AI 生成卡片数据：
   - 对于预定义卡片：使用现有组件
   - 对于新卡片：使用 GenericCard + 配置
```

### 场景3：AI 创建新卡片类型

```
1. AI 分析需要新卡片：
   - 需要"竞品价格对比卡"
   - 现有卡片都不匹配

2. AI 生成新卡片：
   {
     "id": "event-competitor-price-comparison-001",
     "templateId": "event-competitor-price-comparison",  // 新类型
     "componentName": "GenericCard",  // 使用通用组件
     "data": {
       "competitors": [...],
       "prices": [...],
       "trends": [...]
     },
     "config": {
       "layout": "comparison",
       "chart": {
         "type": "bar-chart",
         "data": {...}
       }
     }
   }
```

---

## 🏗️ 实现方案

### 1. 任务配置结构

```json
{
  "id": "task-001",
  "name": "竞品监控",
  "category": "task",
  "version": 1,
  "framework": {
    "monitorScope": {
      "targets": ["竞品A", "竞品B"],
      "keywords": ["价格", "产品", "融资"],
      "channels": ["官网", "社交媒体"]
    },
    "rules": {
      "alertLevels": {
        "high": ["价格调整", "重大产品发布"]
      }
    },
    "summaryCardFramework": {
      // AI 填充：cardIds 数组
      // AI 可以决定数量和种类
    },
    "eventFramework": {
      "requiredCards": ["event-header"],  // 可选：必须包含的卡片
      // AI 填充：其他卡片完全由 AI 决定
    }
  },
  "metadata": {
    "createdAt": "2025-12-01T00:00:00Z",
    "aiGenerated": true  // 标记是否由 AI 生成
  }
}
```

### 2. 事件配置结构

```json
{
  "id": "event-001",
  "name": "竞品A发布低价套餐",
  "category": "event",
  "taskId": "task-001",
  "framework": {
    "cardIds": [
      // AI 生成：完全由 AI 决定
      // 可以是预定义卡片，也可以是新卡片
      "event-header-001",
      "event-quick-read-001",
      "event-competitor-price-comparison-001",  // AI生成的新类型
      "event-market-impact-analysis-001",  // AI生成的新类型
      ...
    ]
  },
  "metadata": {
    "createdAt": "2025-12-04T13:42:00Z",
    "aiGenerated": true,
    "totalCards": 13  // AI 决定的数量
  }
}
```

---

## 🎯 关键设计点

### 1. 框架约束 vs AI 自由度

```
框架约束（必须遵守）：
  ✅ 任务必须有基本配置
  ✅ 事件必须有基本配置
  ✅ 卡片必须符合 CardInstance 格式
  ✅ 卡片必须可渲染

AI 自由度（完全自由）：
  ✅ 卡片数量：1-50张都可以
  ✅ 卡片种类：现有卡片或新卡片
  ✅ 卡片顺序：AI决定
  ✅ 卡片内容：AI生成
```

### 2. 卡片类型系统

```
预定义卡片（高性能）：
  - EventHeaderCard
  - EventCoreInsightCard
  - ...

通用卡片（灵活）：
  - GenericCard（支持多种布局）
  - ChartCard（支持多种图表）
  - TextCard（支持多种文本布局）

AI 生成卡片（完全动态）：
  - 任意 templateId
  - 使用通用组件渲染
  - 通过配置定义布局
```

### 3. 降级机制

```
1. 尝试使用预定义组件（如果 templateId 匹配）
2. 如果不存在，使用通用组件（GenericCard）
3. 如果通用组件也无法渲染，使用默认卡片（InsightCard）
4. 记录错误日志，用于优化
```

---

## 📋 实施步骤

### Step 1: 类型系统改造（立即执行）

```typescript
// lib/future-lens/types/card-types.ts

// ❌ 删除硬编码的联合类型
// export type CardTemplateId = "event-header" | "event-core-insight" | ...

// ✅ 改为完全动态
export type CardTemplateId = string
```

### Step 2: 任务框架系统（立即执行）

```typescript
// lib/future-lens/types/task-types.ts

interface TaskFramework {
  monitorScope: MonitorScope
  rules: TaskRules
  summaryCardFramework?: {
    cardIds?: string[]  // AI 填充
  }
  eventFramework: {
    requiredCards?: string[]  // 可选：必须包含的卡片
  }
}
```

### Step 3: 通用卡片组件（短期）

```typescript
// components/future-lens/cards/generic-card.tsx

export function GenericCard({ data, config }: {
  data: any
  config?: CardConfig
}) {
  // 根据 config 动态渲染
  // 支持多种布局、图表、文本等
}
```

### Step 4: AI 集成（长期）

```
- AI 决策逻辑
- AI 生成配置和数据
- AI 创建新卡片类型
```

---

## ✅ 优势

1. **完全动态**：AI 可以生成任意卡片类型
2. **框架约束**：确保 AI 不会写乱
3. **轻量化**：配置驱动，无需硬编码
4. **AI 友好**：易于 AI 理解和生成
5. **向后兼容**：预定义卡片继续工作

---

## 🎯 总结

### 设计原则

- ✅ **框架约束**：任务和事件有基本框架
- ✅ **AI 自由度**：卡片数量和种类完全由 AI 决定
- ✅ **完全动态**：支持 AI 创建新卡片类型
- ✅ **轻量化**：配置驱动，无需硬编码
- ✅ **降级处理**：确保总能渲染

### 实施优先级

1. **立即执行**：类型系统改造、任务框架系统
2. **短期规划**：通用卡片组件
3. **长期规划**：AI 集成

